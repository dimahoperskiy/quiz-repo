apiVersion: v1
kind: Service
metadata:
  name: geo-service
  namespace: quiz-project
spec:
  selector:
    app: geo-service
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP
