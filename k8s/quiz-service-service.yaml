apiVersion: v1
kind: Service
metadata:
  name: quiz-service
  namespace: quiz-project
spec:
  selector:
    app: quiz-service
  ports:
    - protocol: TCP
      port: 3002
      targetPort: 3002
  type: ClusterIP
